# Queries


## Macbook

distance > 0 && distance < 15 && (title ~ 'macbook' || description ~ 'macbook') && (title ~ 'm1' || description ~ 'm1')

distance > 0 && distance < 15 && (title ~ 'macbook' || description ~ 'macbook') && (title ~ 'm2' || description ~ 'm2')

distance > 0 && distance < 15 && (title ~ 'macbook' || description ~ 'macbook') && (title ~ 'm3' || description ~ 'm3')

distance > 0 && distance < 15 && (title ~ 'macbook' || description ~ 'macbook') && (title ~ 'm4' || description ~ 'm4')


## PC

distance > 0 && distance < 15 && (title ~ 'gamer' || description ~ 'gamer')

distance > 0 && distance < 15 && (title ~ 'pc' || description ~ 'pc') && (title ~ 'nvidia' || description ~ 'nvidia')


## Iphone

distance > 0 && distance < 15 && (title ~ 'iphone' || description ~ 'iphone') && (title ~ '16' || description ~ '16')


## Mac Mini

distance > 0 && distance < 15 && (title ~ 'mac' || description ~ 'mac') && (title ~ 'mini' || description ~ 'mini')